services:
  db:
    container_name: StoreX
    image: "postgres"
    ports:
      - "5433:5432"
    volumes:
      - ./pg-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=Hemant
      - POSTGRES_PASSWORD=Hemant
      - POSTGRES_DB=store-x